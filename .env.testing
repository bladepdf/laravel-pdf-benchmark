# Intentionally contains no credentials. PHPUnit supplies deterministic test
# configuration through phpunit.xml; this file prevents Laravel from falling
# back to a developer's local .env file during automated tests.
